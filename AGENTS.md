# Interface rules

- Every modal uses a single icon-only `×` close button with an accessible label. Do not show “Закрыть”, “Close”, the material title, or other copy in a modal header.
- Modal surfaces follow the application's dark theme variables (`--background`, `--surface`, `--text`, `--muted`, and `--line`).
